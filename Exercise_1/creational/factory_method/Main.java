// Use Case 2: Factory Method Pattern

// Suppose we're building a system that needs to create different types of database connections (e.g., MySQL, PostgreSQL, Oracle). We want to decouple the creation of the connection from the specific database type.

package factory_method;

public class Main {
    
}
